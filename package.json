{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "new-publish": "lerna publish",
    "new-version": "lerna version",
    "new-version2": "lerna version --conventional-commits --yes"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}